Archivos del proyecto LookML que aloja la extensión. Copia `manifest.lkml`, `ca_cap_admin.model.lkml`
y el `bundle.js` compilado (`extension/dist/bundle.js`) a la raíz de un proyecto LookML llamado `ca_cap`.
